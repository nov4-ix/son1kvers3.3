[build]
builder = "nixpacks"

[deploy]
startCommand = "cd apps/the-generator && npm run build && npm start"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"

[env]
NODE_ENV = "production"
